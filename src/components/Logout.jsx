

// const Logout = ({token, setToken }) => {
//     // Remove the token from local storage
//     localStorage.removeItem(token);

//     // Optionally, set the token state to null (if you're using state)
//     setToken(null);
//     console.log('Log-out')
// }
// export default Logout;
